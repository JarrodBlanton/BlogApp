<h1> INDEX PAGE! </h1>

<% blogs.forEach(function(blog) { %>
    <div>
        <h3> <%= blog.title %> </h3>
        <div>
            <img src="<%= blog.image %>" style="width:30%">
        </div>
        <span> <%= blog.created %> </span>
        <p><%= blog.body %></p>
    </div>
<% }) %>

